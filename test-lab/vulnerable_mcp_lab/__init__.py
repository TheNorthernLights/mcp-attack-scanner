"""vulnerable_mcp_lab: a DELIBERATELY VULNERABLE MCP server.

This is a test target for mcp-attack-scanner. It is intentionally insecure by
design and must never be exposed on a network or pointed at real data. See the
README in the `test-lab/` directory.
"""

__version__ = "0.0.1"
